

let initialState = {

    firstName: [
        {id: 1, name: 'Friends'}
    ],

    friends: [
        {id: 2, name: 'Dem'},
        {id: 3, name: 'Flop'},
        {id: 4, name: 'Fes'}
    ],
};

const sidebarReducer = (state = initialState, action) => {

    return state

}

export default sidebarReducer;